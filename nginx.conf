# Nginx 配置 for portal.mubai.top
# 位置：/etc/nginx/sites-available/portal.mubai.top

server {
    listen 80;
    server_name portal.mubai.top;

    # 重定向到 HTTPS (如果需要)
    # return 301 https://$server_name$request_uri;

    location / {
        root /var/www/vue-portal/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # CMDB API 代理
    location /cmdb-api {
        proxy_pass http://localhost:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持 (如果需要)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # 其他 API 代理 (如果需要)
    location /prod-api {
        proxy_pass http://localhost:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;
}

# HTTPS 配置 (推荐生产环境使用)
# server {
#     listen 443 ssl http2;
#     server_name portal.mubai.top;
#
#     ssl_certificate /etc/letsencrypt/live/portal.mubai.top/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/portal.mubai.top/privkey.pem;
#     ssl_trusted_certificate /etc/letsencrypt/live/portal.mubai.top/chain.pem;
#
#     # SSL 配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # 其他配置同上...
# }
